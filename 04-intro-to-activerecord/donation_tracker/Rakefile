require_relative './config/environment'
require 'sinatra/activerecord/rake'


desc "Baby Shark"
task :babyshark do
    puts "baby 🦈 do do dooo do"
    puts "baby 🦈 do do dooo do"
end

desc "Runs a Pry console"
task :console do
    #This line turns on logging of SQL generated by Active Record 
    ActiveRecord::Base.logger = Logger.new(STDOUT)
    #Opens a Pry session
    Pry.start 
end 
